services:
  linux-x11-harness:
    build: .
    container_name: linux-x11-harness
    # Run the long-lived daemon; agents exec in with `linux-x11-harness mcp`.
    command: ["serve"]
    environment:
      - DISPLAY=:99
    # Share host IPC for better X11/Shm performance (optional).
    ipc: host
    # No ports exposed; MCP uses stdio via docker exec.
